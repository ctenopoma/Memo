## 知識管理とデータアーキテクチャ (Knowledge Management & Data Structure)

システム内で情報をどのように構造化し、保存しているか（オントロジーとストレージ設計）について

### オントロジーの構築手法

LLM wikiにおいて、オントロジー（概念体系）は具体的にどのように構築・管理されているか？

Markdownファイル自体の種別（メタデータ等）や、Markdownファイル同士の関係性（リンクや階層）はどのように定義・保持されているか？

### データ保存とインフラ設計

Markdownファイルはどのようなファイルシステム・データベースで管理・保存しているか？

ベクトル検索用の「ベクトルDB」や、メタデータ・関係性管理用の「リレーショナルDB (RDB) / グラフDB」など、複数のDBを併用したハイブリッドなアーキテクチャを組んでいるか？

Gitなどのバージョン管理は適用可能か？

##  検索技術とRAGアプローチ (Search & Retrieval)
必要な情報をどのように高精度で引き出すか、他手法との差別化について

### 検索ロジックへのドメイン知識の反映

単純な類似度検索だけでなく、業務ノウハウや特定の検索手順（クエリの拡張やフィルタリング等）は、システムにどのように組み込まれているか？

### GraphRAGとの差別化

昨今注目されているグラフRAG（知識グラフを用いたRAG）と比較して、貴社のアプローチ（LLM wikiによる関係性定義など）の優位性や差別化ポイントはどこにあるか？

## 分析プロセスとレポート生成の自動化 (Analysis & Report Generation)

抽出したデータをどのように加工・分析し、最終的なレポートに落とし込むかについて

### 分析のアプローチ

- 定型分析: あらかじめ準備したPythonスクリプト等で処理しているか？

- 非定型分析: LLM自身の推論（プロンプト上での計算等）に任せているか、それともLLMにPythonコードを動的に生成・実行（Code Interpreter等）させているか？

### レポート構成と出力形式

抽出結果をLLMに「再構築（レポート化）」させる処理は不要か？（Wiki自体がレポート構造になっており、それをプロンプトに積むだけで完了する設計か？）

分析結果として生成されたグラフや表などのリッチコンテンツは、最終出力（Markdown等）にそのまま動的に埋め込み可能か？

最終出力は人も読める形式であるか？

継続的な更新が可能な構造になっているか？


### 大規模レポート生成時の品質担保

分析対象が膨大で1回の推論（コンテキストウィンドウ）に収まらない場合、どのようなアプローチをとっているか？（ページやセクションごとに生成を分割する等）

分割生成する場合、レポート全体を通した文脈の一貫性や論理展開の破綻をどう防いでいるか？

## 制約下でのLLM活用とアーキテクチャ (LLM Strategy & Constraints)
LLMの弱点や制約をシステム全体でどうカバーしているかについて

### トークン制限・計算資源の制約

ローカルLLMなど、コンテキストウィンドウ（トークン制限）が厳しい環境下では、どのようなチャンク分割や情報圧縮の工夫をしているか？

## LLMと外部ツールの役割分担・設計思想 (System Orchestration & Philosophy)
LLMの特性をどう捉え、システム全体のどこに配置しているか（エージェント設計）について

### LLMの特性を活かしたアーキテクチャ

LLMの「確率的出力（ばらつき）」と「事前知識」をどう制御しているか？

ビジネスアイデア等の「正解のない分析」において、LLMのワンショット推論（1回の回答）の限界をどう乗り越えているか？

提案・確認: 確実な処理（事前スクリプト）と、複数LLMによるマルチエージェントでの議論・アンサンブル（多数決や合意形成）を組み合わせて出力を収束させるようなアプローチをとっているか？

## システム全体のオーケストレーション (★)

「LLMに任せる領域（推論・生成）」と「外部ツールに任せる領域（計算・検索・実行）」の境界線をどのように定義・整理しているか？

今回の依頼範囲における全体的なアーキテクチャや方針（システムとしてどう実装されているか）を、開示可能な範囲で教えてもらえるか？